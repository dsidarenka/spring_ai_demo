# Spring AI Demo
Spring AI Demo For Java Poland Group Tech Talk

## Zipkin server

In order to view traces reported by Micrometer library, please run the Zipkin server locally via this command:

```shell
docker run -d -p 9411:9411 openzipkin/zipkin
```